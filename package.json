{
  "name": "peppol-quest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute peppol-quest --file=./schema.sql",
    "typecheck": "echo 'No TS typecheck configured yet'"
  },
  "devDependencies": {
    "wrangler": "^4.10.0"
  }
}
